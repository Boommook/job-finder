import{getJobs}from"@/lib/jobs";import{JobsDashboard}from"@/components/jobs-dashboard";import{requireUser}from"@/lib/auth";import type{JobQuery,JobStatus,WorkArrangement}from"@/types/job";
export default async function JobsPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const[user,params]=await Promise.all([requireUser(),searchParams]);const query:JobQuery={page:Number(params.page)||1,pageSize:50,search:params.search,status:(params.status??"all")as "all"|JobStatus,company:params.company,source:params.source,arrangement:(params.arrangement??"all")as "all"|WorkArrangement,sort:(params.sort??"discovered")as JobQuery["sort"]};return <JobsDashboard result={await getJobs(user.id,query)} query={query}/>}
