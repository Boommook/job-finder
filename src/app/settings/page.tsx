import{requireUser}from"@/lib/auth";import{getSettings}from"@/app/actions";import{SettingsForm}from"@/components/settings-form";
export default async function SettingsPage(){const user=await requireUser(),settings=await getSettings(user.id);return <div className="mx-auto max-w-5xl"><header><p className="text-sm font-semibold text-[#335cff]">Ranking profile</p><h1 className="mt-1 text-2xl font-bold tracking-tight sm:text-3xl">Preferences & skills</h1><p className="mt-1 text-sm text-slate-500">Keep your personal details and job-search preferences up to date.</p></header><SettingsForm initial={settings}/></div>}
