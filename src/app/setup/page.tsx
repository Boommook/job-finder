export default function SetupPage(){return <div className="panel mx-auto max-w-xl p-8"><h1 className="text-2xl font-bold">Supabase setup required</h1><p className="mt-3 text-sm leading-6 text-slate-600">Add the following variables to <code>.env.local</code>, then restart the development server:</p><pre className="mt-4 overflow-auto rounded-lg bg-slate-900 p-4 text-sm text-slate-100">NEXT_PUBLIC_SUPABASE_URL={"\n"}NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=</pre><p className="mt-4 text-sm text-slate-500">Run the migration and seed SQL documented in the README before signing in.</p></div>}
