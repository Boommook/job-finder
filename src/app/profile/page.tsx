import { requireUser } from "@/lib/auth";import { getCandidateData } from "@/lib/candidate/repository";import { CandidateProfileEditor,ImportedProfileReview } from "@/components/candidate-profile";
export default async function ProfilePage(){const user=await requireUser(),data=await getCandidateData(user.id);return <div className="mx-auto max-w-5xl"><header><p className="text-sm font-semibold text-[#335cff]">Candidate evidence</p><h1 className="mt-1 text-2xl font-bold tracking-tight sm:text-3xl">Candidate profile</h1><p className="mt-1 text-sm text-slate-500">Review résumé imports and maintain the facts used for personalized evaluation.</p></header><ImportedProfileReview data={data}/><CandidateProfileEditor data={data}/></div>}
