import { BriefcaseBusiness, Sparkles, ThumbsUp, Bookmark, Send } from "lucide-react";
import type { Job } from "@/types/job";
export function DashboardStats({jobs}:{jobs:Job[]}){const items=[{label:"Active jobs",value:jobs.length,icon:BriefcaseBusiness},{label:"New jobs",value:jobs.filter(j=>j.status==="new").length,icon:Sparkles},{label:"Recommended",value:jobs.filter(j=>(j.evaluation?.overallScore??0)>=80).length,icon:ThumbsUp},{label:"Saved",value:jobs.filter(j=>j.status==="saved").length,icon:Bookmark},{label:"Applied",value:jobs.filter(j=>j.status==="applied").length,icon:Send}];return <section aria-label="Job summary" className="grid grid-cols-2 gap-3 lg:grid-cols-5">{items.map(({label,value,icon:Icon})=><div className="panel flex items-center gap-3 p-4" key={label}><span className="grid size-9 shrink-0 place-items-center rounded-lg bg-slate-100 text-slate-600"><Icon size={17}/></span><span><strong className="block text-xl leading-none tabular-nums">{value.toLocaleString()}</strong><small className="mt-1 block text-xs font-medium text-slate-500">{label}</small></span></div>)}</section>}
