"use client";import{useMemo,useState}from"react";import{useSearchParams}from"next/navigation";import type{Job,JobStatus}from"@/types/job";import{DashboardStats}from"./dashboard-stats";import{defaultFilters,JobFilters,type Filters}from"./job-filters";import{JobCard}from"./job-card";import{EmptyState}from"./ui";
export function JobsDashboard({initialJobs}:{initialJobs:Job[]}){const params=useSearchParams();const initialStatus=params.get("status") as JobStatus|null;const[allJobs,setAllJobs]=useState(initialJobs);const[filters,setFilters]=useState<Filters>({...defaultFilters,status:initialStatus??"all"});const[limit,setLimit]=useState(18);const categories=useMemo(()=>[...new Set(allJobs.map(j=>j.category))].sort(),[allJobs]);const locations=useMemo(()=>[...new Set(allJobs.map(j=>j.location))].sort(),[allJobs]);const jobs=useMemo(()=>{const q=filters.search.toLowerCase().trim();return allJobs.filter(j=>(!q||[j.title,j.company,j.location,j.evaluation?.matchingSkills.join(" ")].join(" ").toLowerCase().includes(q))&&(filters.category==="all"||j.category===filters.category)&&(filters.location==="all"||j.location===filters.location)&&(filters.arrangement==="all"||j.workArrangement===filters.arrangement)&&(filters.status==="all"||j.status===filters.status)&&(j.evaluation?.overallScore??0)>=filters.minScore).sort((a,b)=>filters.sort==="match"?(b.evaluation?.overallScore??0)-(a.evaluation?.overallScore??0):new Date(filters.sort==="posted"?b.postedAt??0:b.discoveredAt).getTime()-new Date(filters.sort==="posted"?a.postedAt??0:a.discoveredAt).getTime())},[allJobs,filters]);const changeStatus=(id:string,status:JobStatus)=>setAllJobs(current=>current.map(j=>j.id===id?{...j,status}:j));const reset=()=>setFilters(defaultFilters);return <><header className="mb-6"><p className="text-sm font-semibold text-[#335cff]">Opportunity pipeline</p><h1 className="mt-1 text-2xl font-bold tracking-tight sm:text-3xl">Job discovery</h1><p className="mt-1 text-sm text-slate-500">Review and triage matches from your latest mock scan.</p></header><DashboardStats jobs={allJobs}/><div className="mt-5"><JobFilters filters={filters} onChange={f=>{setFilters(f);setLimit(18)}} categories={categories} locations={locations} onReset={reset}/></div><div className="mb-3 mt-5 flex items-center justify-between"><p className="text-sm text-slate-500"><strong className="text-slate-800">{jobs.length.toLocaleString()}</strong> matching jobs</p><span className="text-xs text-slate-400">Showing {Math.min(limit,jobs.length)}</span></div>{jobs.length?<div className="space-y-3">{jobs.slice(0,limit).map(job=><JobCard key={job.id} job={job} onStatus={changeStatus}/>)}{limit<jobs.length&&<button className="btn btn-secondary mx-auto flex" onClick={()=>setLimit(v=>v+18)}>Load more jobs</button>}</div>:<EmptyState onReset={reset}/>}</>}
