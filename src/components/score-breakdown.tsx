import type { JobEvaluation } from "@/types/job";
const dimensions:[keyof JobEvaluation,string][]=[["skillScore","Skills"],["experienceScore","Experience"],["interestScore","Role interest"],["locationScore","Location"],["educationScore","Education eligibility"],["companyScore","Company / industry"],["compensationScore","Compensation"]];
export function ScoreBreakdown({evaluation}:{evaluation:JobEvaluation}){return <div className="space-y-4">{dimensions.map(([key,label])=>{const value=evaluation[key] as number;return <div key={key}><div className="mb-1.5 flex justify-between text-sm"><span className="font-medium text-slate-700">{label}</span><strong className="tabular-nums">{value}/100</strong></div><div className="h-2 overflow-hidden rounded-full bg-slate-100" role="progressbar" aria-label={label} aria-valuenow={value} aria-valuemin={0} aria-valuemax={100}><div className="h-full rounded-full bg-[#4367ee]" style={{width:`${value}%`}}/></div></div>})}</div>}
