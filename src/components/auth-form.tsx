"use client";import{useFormStatus}from"react-dom";import Link from"next/link";import{Radar}from"lucide-react";
function Submit({label}:{label:string}){const{pending}=useFormStatus();return <button className="btn btn-primary mt-2 w-full" disabled={pending}>{pending?"Please wait…":label}</button>}
export function AuthForm({mode,action,error,message}:{mode:"login"|"signup";action:(data:FormData)=>void|Promise<void>;error?:string;message?:string}){const signup=mode==="signup";return <div className="w-full max-w-md"><div className="mb-7 flex items-center justify-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#335cff] text-white"><Radar size={22}/></span><span className="text-lg font-bold">Job Finder</span></div><section className="panel p-6 sm:p-8"><h1 className="text-2xl font-bold tracking-tight">{signup?"Create your account":"Welcome back"}</h1><p className="mt-1 text-sm text-slate-500">{signup?"Start building your personal opportunity pipeline.":"Sign in to view your saved jobs and applications."}</p>{error&&<p role="alert" className="mt-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}{message&&<p className="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700">{message}</p>}<form action={action} className="mt-5 space-y-4">{signup&&<Field name="fullName" label="Full name" autoComplete="name"/>}<Field name="email" label="Email" type="email" autoComplete="email" required/><Field name="password" label="Password" type="password" autoComplete={signup?"new-password":"current-password"} minLength={signup?8:undefined} required/><Submit label={signup?"Create account":"Sign in"}/></form><p className="mt-5 text-center text-sm text-slate-500">{signup?"Already have an account? ":"New to Job Finder? "}<Link className="font-semibold text-[#335cff] hover:underline" href={signup?"/login":"/signup"}>{signup?"Sign in":"Create an account"}</Link></p></section></div>}
function Field(props:React.InputHTMLAttributes<HTMLInputElement>&{label:string}){const{label,...input}=props;return <label className="block text-sm font-semibold text-slate-700">{label}<input {...input} className="control mt-1.5 w-full"/></label>}
