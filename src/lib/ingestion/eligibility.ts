import type{Job,JobPreferences}from"@/types/job";import type{EligibilityResult}from"./types";
export function evaluateEligibility(job:Job,preferences:JobPreferences):EligibilityResult{const reasons:string[]=[];if(job.status==="rejected")reasons.push("Job was rejected");if(!job.isActive)reasons.push("Posting is closed");if(job.employmentType&&preferences.employmentTypes.length&&!preferences.employmentTypes.includes(job.employmentType))reasons.push(`Employment type ${job.employmentType} is excluded`);const haystack=`${job.title}\n${job.company}\n${job.description}`.toLowerCase();for(const keyword of preferences.excludedKeywords){if(keyword.trim()&&haystack.includes(keyword.trim().toLowerCase()))reasons.push(`Contains excluded keyword: ${keyword}`)}if(job.workArrangement&&preferences.remotePreference!=="flexible"&&job.workArrangement!==preferences.remotePreference)reasons.push(`Workplace type ${job.workArrangement} does not match ${preferences.remotePreference}`);if(preferences.minimumSalary!==null&&job.salaryMin!==undefined&&job.compensationInterval==="yearly"&&job.salaryMin<preferences.minimumSalary)reasons.push("Known yearly compensation is below minimum");if(preferences.sponsorshipRequired&&job.sponsorship===false)reasons.push("Posting explicitly does not offer sponsorship");return{eligible:reasons.length===0,reasons}}
