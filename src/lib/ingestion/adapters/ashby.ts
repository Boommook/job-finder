import type { JobSourceAdapter,NormalizedJobInput } from "../types";import{asNumber,asString,fetchJson,isRecord,raw}from"./shared";import{contentHash,explicitSponsorship,inferSeniority,normalizeEmployment,normalizeInterval,normalizeWorkplace,textFromHtml}from"../normalize/common";
export const ashbyAdapter:JobSourceAdapter={source:"ashby",async fetchJobs(source,signal){const payload=await fetchJson(`https://api.ashbyhq.com/posting-api/job-board/${encodeURIComponent(source.boardIdentifier)}`,signal);if(!isRecord(payload)||!Array.isArray(payload.jobs))throw new Error("Unexpected Ashby response");return payload.jobs.filter(isRecord).map((job):NormalizedJobInput=>{const id=asString(job.id),title=asString(job.title),url=asString(job.jobUrl);if(!id||!title||!url)throw new Error("Ashby posting is missing identity fields");const html=asString(job.descriptionHtml)??"",description=asString(job.descriptionPlain)??textFromHtml(html),location=asString(job.location)??"Location unavailable",comp=isRecord(job.compensation)?job.compensation:undefined,min=comp?asNumber(comp.min):undefined,max=comp?asNumber(comp.max):undefined;return{externalId:id,provider:"ashby",company:source.companyName,title,description,location,workplaceType:normalizeWorkplace(asString(job.workplaceType),location),employmentType:normalizeEmployment(asString(job.employmentType)),applicationUrl:asString(job.applyUrl),sourceUrl:url,postedAt:asString(job.publishedAt),discoveredAt:new Date().toISOString(),skills:[],salaryMin:min,salaryMax:max,salaryCurrency:comp?asString(comp.currency):undefined,compensationInterval:normalizeInterval(comp?asString(comp.interval):undefined),seniority:inferSeniority(title),sponsorship:explicitSponsorship(description),rawPayload:raw(job),contentHash:contentHash({company:source.companyName,title,location,url})}})}};
