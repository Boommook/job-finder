import type{JobSourceAdapter,NormalizedJobInput,SourceConfig}from"../types";import{asString,fetchJson,isRecord,raw,validPostings}from"./shared";import{contentHash,explicitSponsorship,inferSeniority,normalizeEmployment,normalizeWorkplace,textFromHtml}from"../normalize/common";
export function parseGreenhouseJobs(payload:unknown,source:SourceConfig,warn?:((message:string)=>void)){if(!isRecord(payload)||!Array.isArray(payload.jobs))throw new Error("Unexpected Greenhouse response");return validPostings(payload.jobs,"Greenhouse",job=>{const identity=job.id,title=asString(job.title),url=asString(job.absolute_url),id=typeof identity==="number"||typeof identity==="string"?String(identity):undefined;if(!id||!title?.trim()||!url?.trim())return;const location=isRecord(job.location)?asString(job.location.name)??"Location unavailable":"Location unavailable",description=textFromHtml(asString(job.content)??"");return{externalId:id,provider:"greenhouse",company:source.companyName,title,description,location,workplaceType:normalizeWorkplace(undefined,location),employmentType:normalizeEmployment(title),applicationUrl:url,sourceUrl:url,postedAt:asString(job.updated_at),discoveredAt:new Date().toISOString(),skills:[],compensationInterval:"unknown",seniority:inferSeniority(title),sponsorship:explicitSponsorship(description),rawPayload:raw(job),contentHash:contentHash({company:source.companyName,title,location,url})} satisfies NormalizedJobInput},warn)}
export const greenhouseAdapter:JobSourceAdapter={source:"greenhouse",async fetchJobs(source,signal){return parseGreenhouseJobs(await fetchJson(`https://boards-api.greenhouse.io/v1/boards/${encodeURIComponent(source.boardIdentifier)}/jobs?content=true`,signal),source)}};
